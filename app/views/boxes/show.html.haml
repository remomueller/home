%h1.display-3
  Box
  = @box.name

- if @box.closed?
  %p.text-muted Closed
- else
  %p Open

= link_to 'Edit', edit_box_path(@box)
|
= link_to 'Back', boxes_path

%p
  %strong Description
  = @box.description



%p
  %strong Items
  = @box.items.count

  %ul
    - @box.items.order('lower(name)').each do |item|
      %li= item.name
